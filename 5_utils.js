const sayHi = (name) => {
    console.log(`Hello there ${name}`)
  }

  const sayHi2 = (name) => {
    console.log(`Hello there from Hi2 ${name}`)
  }
  // export default
  module.exports = sayHi
  
  // export default
  